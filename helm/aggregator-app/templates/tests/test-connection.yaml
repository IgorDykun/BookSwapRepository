apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aggregator-app.fullname" . }}-test-connection"
  labels:
    {{- include "aggregator-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aggregator-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
